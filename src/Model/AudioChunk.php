<?php

declare(strict_types=1);

namespace App\Model;

/**
 * Dummy placeholder for a finalized recording chunk (e.g. path to WAV).
 * No properties yet; used so RecorderFlow can emit an output IP when stop completes.
 */
final readonly class AudioChunk
{
}
