framework:
    messenger:
        # Transports are created dynamically per session in VoiceTransportProvider
        serializer:
            default_serializer: messenger.transport.native_php_serializer
